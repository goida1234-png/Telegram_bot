services:
  - type: web
    name: quiz-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false # вводим вручную в Render dashboard
      - key: WEBHOOK_HOST
        sync: false # вводим вручную в Render dashboard
